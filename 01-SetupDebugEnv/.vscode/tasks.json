{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run_qemu",
            "type": "shell",
            "isBackground": true,
            "command": "bash ${workspaceFolder}/.vscode/start-qemu.sh ${workspaceFolder}"
        }
    ]
}